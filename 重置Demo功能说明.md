# 重置Demo功能说明

## 功能概述

在网页右上角添加了一个"重置Demo"按钮，用于快速重置演示数据，清空所有数据表并重新加载初始数据。

## 功能特点

### 前端功能
- 位置：网页右上角，在"达人筛号系统"标题旁边
- 样式：红色按钮，带有重置图标
- 交互：点击后会弹出确认对话框，防止误操作
- 反馈：操作完成后显示成功消息，并自动刷新页面

### 后端功能
- API端点：`POST /api/data/reset-demo`
- 清空的数据表：
  - `sys_influencer` - 达人信息表
  - `sys_influencer_style_tags` - 达人风格标签表（关联表）
  - `sys_influencer_prediction_reasons` - 达人预测原因表（关联表）
  - `scheduler_config` - 定时任务配置表
- 重新加载：清空后自动从资源文件重新加载初始数据

## 使用方法

1. 打开达人筛号系统网页
2. 在右上角找到"重置Demo"按钮
3. 点击按钮
4. 在弹出的确认对话框中点击"确认重置"
5. 等待操作完成，系统会显示成功消息
6. 页面会自动刷新，显示重置后的数据

## 安全措施

- 确认对话框：防止误操作
- 操作日志：后端会记录重置操作的详细日志
- 错误处理：如果重置失败，会显示具体的错误信息

## 技术实现

### 前端实现
- 使用 Ant Design 的 Modal.confirm 组件实现确认对话框
- 使用 message 组件显示操作结果
- 操作完成后自动刷新页面以更新数据

### 后端实现
- 使用 Spring Data JPA 的 deleteAll() 方法清空数据表
- 调用 DataImportService 重新加载初始数据
- 返回详细的操作结果，包括清空的表和重新加载的数据量

## 注意事项

- 此功能会清空所有数据，请谨慎使用
- 重置操作不可逆，请确保在演示环境中使用
- 操作完成后页面会自动刷新，请保存好当前的工作状态